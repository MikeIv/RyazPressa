---
description: Execute — дисциплина реализации (Superpowers)
alwaysApply: true
---

# Execute: как писать код

## До правок

1. Прочитать релевантные файлы и соседний код
2. Свериться с `nuxt-template.mdc`
3. Свериться с `brief.md` / планом текущей волны

## Во время правок

- **Одна логическая задача** за итерацию
- **Минимальный diff** — без drive-by рефакторинга
- SFC: `script setup` → `template` → `style module` (если есть стили)
- API: `useApi` / `useApiFetch` / `serverApi`, не дублировать сырой `$fetch` с `baseURL`
- Не коммитить и не пушить без явной просьбы

## Порядок

1. Типы / composables (для новой функциональности и **L+** — согласовать контракты в Discuss, см. `06-fundamental-principles.mdc` §3)
2. Компоненты / страницы
3. Стили через `--fs-*` из `app/assets/styles/`

## Отчёт после волны

```markdown
## Сделано / Файлы / Проверено / Дальше
```

## Застрял

- Не угадывать 4+ раза — гипотеза, проверка, блокер пользователю
